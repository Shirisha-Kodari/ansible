# create 3 servers and install nginx each server at a time 
# prerequests: 
* create one server and install ansible 
* first create servers with asible 
* write nginx.yaml file 
* setup inventory file 
* 
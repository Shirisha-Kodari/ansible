# - name: ping the server  # name of the playbook 
#   hosts: frontend
#   tasks: #list of tasks  
#   - name: ping the server
#     ansible.builtin.ping:  # command 

- name: server 
  hosts: localhost
  hosts: ansible  
  tasks:
  - name: ping the server 
    ansible.builtin.ping: 